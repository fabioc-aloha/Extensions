{
    "name": "@alex-extensions/shared",
    "version": "0.1.0",
    "private": true,
    "description": "Shared utilities for Alex extension family",
    "main": "out/index.js",
    "types": "out/index.d.ts",
    "scripts": {
        "compile": "tsc -p tsconfig.json",
        "watch": "tsc -p tsconfig.json --watch"
    },
    "devDependencies": {
        "typescript": "^5.7.0"
    }
}
